# 2026-09-09 재개·배포 검증

이전 작업의 추가 기사 배포와 VIP 뉴스 6개·오픈소스 2개 준비 요청을 이어서 처리했습니다.
이 문서는 커밋 전 검증 기록이며, 실제 운영 배포 결과는 Vercel 배포 이력을 기준으로 합니다.

## 반영 범위

| 주차 | 이전 | 현재 | 추가 |
|---|---:|---:|---:|
| 2026-w35 | 24 | 102 | 78 |
| 2026-w36 | 23 | 143 | 120 |
| 2026-w37 | 5 | 13 | 8 |
| 합계 | 52 | 258 | 206 |

기사·원문 근거를 유지하며 공유 미리보기 문구를 제거했습니다. 자체 편집 이미지 고지는 유지합니다.
이번 재개에서 영문판 검사 도구의 TypeScript 로딩 호환성과 652px에서 메뉴 단어가 쪼개지는 문제를 수정했습니다.
헤더는 820px부터 두 행으로 배치하고 작은 휴대폰에서 보조 Archive 표기만 숨깁니다.

## 직접 실행한 검증

- `vercel build --prod`: exit 0. Next.js 정적 페이지 1,153개 생성, 생성 HTML 1,151개 누수 검사 0건.
- 출처 통합 검사: 현재 258건, 기존 11건, 누적 백필 247건, 근거 파일 493개 정합성 확인.
- `npm run test:archive`: exit 0. 실제 25회차, 이미지 기록 1,100개 검사.
- 원장 검증기의 오류 차단 사례 61개: PASS.
- 영문판 25주차 및 내장 TypeScript 실행 기능 비활성 환경: PASS.
- `npm run test:browser`: exit 0. 주간 3개·AB 1개를 1440/1024/820/768/652/390px에서 검사.
- 브라우저: 상단 텍스트 분절, 언어·필터·기사 이동·이전 링크·시간대 수화 오류 없음.
- 이미지 표시: 5개 경로의 1440/390px 고지, 대체 이미지 실패, 위험 URL 거부 검사 통과.
- 독립 기사 258개 전부 로컬 HTTP 200, 단일 제목, canonical 확인.
- 실제 652px 브라우저 화면에서 수정 전후 상단 메뉴를 확인했습니다.
- TypeScript 컴파일과 변경 JavaScript 문법 검사 통과. 별도 LSP 서버는 설치되어 있지 않아 LSP 진단은 실행하지 못했습니다.

## 배포 방법과 공개 범위

`gates/verify_voidnews.sh`는 기존 검사들을 단일 진입점으로 연결합니다. 모든 하위 검사의 비정상 종료는 배포 검증 실패입니다.
`close.sh`의 종료코드, 비밀값 검사 및 화면 검증 후 생성된 `.vercel/output`을 `--prebuilt --prod --archive=tgz`로 배포합니다.
비공개 환경 파일·원문 캐시·VIP 준비 문서는 배포 파일에 포함하지 않습니다.
VIP 준비본은 최종 선정·게시 전 별도 로컬 자료로 유지합니다.

## 기존 제한

- 과거 회차의 미해결 이미지 62개는 기존 승인 목록으로 추적 중입니다. 신규 미해결 0개, 로컬 이미지 참조 482개 실존.
- 기존 카드의 deck 필드 누락 283개는 현재 경고 대상입니다. 신규 데이터 검사와 별개로 원래 남아 있던 범위입니다.
- 전체 채널의 모든 글을 전수 조사했다는 뜻은 아닙니다. 접근 제한과 원문 미검토 후보는 ROTATION-COVERAGE.md에 남깁니다.
- 원문 캐시는 로컬에 보존되어 있으므로 원장 전체 검증과 운영용 빌드는 해당 근거를 가진 작업 환경에서 수행합니다.
- 이미지 오류 경로 테스트는 외부 응답을 고정한 검사이며, 외부 이미지 서비스의 지속 가용성을 보증하지 않습니다.
