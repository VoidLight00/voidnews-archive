# 최근 2주 백필 최종 통합 검증

대상은 2026-08-26~2026-09-08입니다. 정확한 시각이 있는 자료는 KST로 환산했습니다. 날짜만 확인된 5건은 발행사 달력 날짜이며 시간대 미확인을 한영 본문과 원장에 표시했습니다. 모든 AI 소식을 망라했다는 의미는 아닙니다.

## 최종 반영

- 신규 기사 41건을 반영했습니다. 기존 11건을 보존해 w35 24건, w36 23건, w37 5건입니다.
- w37을 최신 회차로 등록했습니다. 9월 7일 2건, 9월 8일 3건을 채웠습니다.
- 신규 기사는 Web 출처 유형, KO/EN 요약·본문, 독립 기사 slug를 사용합니다.
- Mistral은 발행사 날짜 확인으로 보류를 해소했습니다. Anthropic 4건도 같은 정밀도 구분을 유지합니다.
- 인수 합의·제한 제공·순차 배포·성능 주장의 범위를 확대하지 않았습니다.
- 공식 글과 연결된 공유 미리보기 12건을 추가했습니다. 기사 첫 이미지, 육안 검수 또는 재사용 권리를 검증했다고 표시하지 않습니다.

## 최종 실행 결과

- `npx tsc --noEmit`: 통과했습니다.
- `npm run test:archive`: 25회차, 894개 이미지 카드 기록을 검사했습니다. 공유 미리보기 20건이 출처·경로·해시 원장과 일대일로 대응합니다.
- `node scripts/check-backfill-integration.mjs`: 현재 기사 52건, 기존 11건, 백필 41건, 근거 파일 70개 대응을 확인했습니다. 25개 주간·12개 AB 레지스트리를 검사했습니다.
- `npm run build`: 종료코드 0, 정적 페이지 947개 생성, 마크다운 누수 0건입니다.
- `npm run test:browser`: 종료코드 0입니다. w35/w36/w37/AB를 1440·1024·390px에서 검사했고, 서울·로스앤젤레스 시간대의 시계 변경 조건에서도 수화 검사를 통과했습니다.
- 독립 기사 52개를 모두 방문해 HTTP 200, h1 1개, canonical 1개를 확인했습니다. 브라우저 실행 오류 0건입니다.
- `git diff --check`: 통과했습니다.

## 발견한 실패와 재검사

과거 피드 수 10개·미리보기 8개를 고정한 테스트는 새 데이터 수와 불일치했습니다. 현재 데이터와 실제 증거 원장을 대조하도록 수정하고 재검사했습니다.

React #418 수화 불일치를 수정한 뒤 시계·시간대 회귀 검사와 전체 브라우저 검사를 통과했습니다. 오류 숨김으로 통과시킨 것이 아닙니다.

새 provenance 게이트가 빌드의 `--warn` 옵션을 파일 경로로 읽어 한 차례 빌드가 실패했습니다. 옵션과 위치 인수를 구분하도록 수정했고 엄격한 검증과 최종 빌드를 다시 통과했습니다.

## 근거와 남은 외부 제한

- 최종 대응 원장: `docs/redesign/BACKFILL-INTEGRATION.json`
- 경계 후보 원장: `docs/redesign/BOUNDARY-BACKFILL.json`
- 추가 이미지 원장: `docs/redesign/BACKFILL-IMAGE-EVIDENCE.json`
- 실제 원문·추출 기록은 `_workspace/oneway/`의 관련 백필 run 아래에 보존했습니다. 이 경로는 Git ignore 대상입니다.
- Anthropic 4건은 부분 WebFetch 추출 기록이며 원본 HTML 전체 캐시가 아닙니다. 일부 전반 구간 항목은 RSS와 수집 근거만 보존돼 있습니다. 오프라인 대응 검사가 모든 번역 주장의 독립 재검증을 뜻하지는 않습니다.
- OpenAI 후보 5건은 본문 HTTP 403으로 미반영입니다. 9월 6일 후보 2건도 포함합니다. 소식 부재가 아닌 본문 검증 제한입니다.
- 현재 세 회차의 35개 게시물에는 명시적 썸네일이 없습니다. 기존 OG·텍스트 대체 동작을 유지하며 공식 첫 이미지를 전수 인증했다고 주장하지 않습니다.

기존 승인 대기 AB 실행은 변경하지 않았습니다. 커밋·푸시·운영 배포는 하지 않았습니다.
